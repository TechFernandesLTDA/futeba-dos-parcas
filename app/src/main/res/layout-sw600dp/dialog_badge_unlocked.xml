<?xml version="1.0" encoding="utf-8"?>
<!-- Layout para tablets (sw600dp+) - Dialog de badge maior e centralizado -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:maxWidth="480dp"
        android:padding="40dp"
        android:background="@drawable/badge_rarity">

        <!-- Título -->
        <TextView
            android:id="@+id/tvUnlockedTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/dialog_badge_unlocked_text_1"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:gravity="center"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Badge Icon com Background - Maior em tablets -->
        <FrameLayout
            android:id="@+id/flBadgeIcon"
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:layout_marginTop="32dp"
            app:layout_constraintTop_toBottomOf="@id/tvUnlockedTitle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Background Circular -->
            <View
                android:id="@+id/vBadgeBackground"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_badge_circle" />

            <!-- Ícone -->
            <TextView
                android:id="@+id/tvBadgeIcon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@string/dialog_badge_unlocked_text_2"
                android:textSize="80sp" />

        </FrameLayout>

        <!-- Nome da Badge -->
        <TextView
            android:id="@+id/tvBadgeName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/dialog_badge_unlocked_text_3"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:gravity="center"
            app:layout_constraintTop_toBottomOf="@id/flBadgeIcon" />

        <!-- Descrição -->
        <TextView
            android:id="@+id/tvBadgeDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/dialog_badge_unlocked_text_4"
            android:textSize="16sp"
            android:textColor="@android:color/white"
            android:alpha="0.9"
            android:gravity="center"
            app:layout_constraintTop_toBottomOf="@id/tvBadgeName" />

        <!-- XP Reward -->
        <TextView
            android:id="@+id/tvXpReward"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/dialog_badge_unlocked_text_5"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFD700"
            android:paddingHorizontal="20dp"
            android:paddingVertical="10dp"
            android:background="@drawable/badge_secondary"
            app:layout_constraintTop_toBottomOf="@id/tvBadgeDescription"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Botão Fechar -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnClose"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="32dp"
            android:text="@string/dialog_badge_unlocked_text_6"
            android:textColor="?attr/colorPrimary"
            android:textSize="16sp"
            style="@style/Widget.Material3.Button"
            app:backgroundTint="@android:color/white"
            app:layout_constraintTop_toBottomOf="@id/tvXpReward" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</FrameLayout>
